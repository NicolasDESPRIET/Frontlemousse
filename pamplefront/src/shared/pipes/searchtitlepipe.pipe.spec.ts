import { SearchtitlepipePipe } from './searchtitlepipe.pipe';

describe('SearchtitlepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtitlepipePipe();
    expect(pipe).toBeTruthy();
  });
});
