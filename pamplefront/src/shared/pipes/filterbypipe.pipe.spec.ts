import { FilterbypipePipe } from './filterbypipe.pipe';

describe('FilterbypipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbypipePipe();
    expect(pipe).toBeTruthy();
  });
});
