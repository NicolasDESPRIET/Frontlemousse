import { SortbypipePipe } from './sortbypipe.pipe';

describe('SortbypipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortbypipePipe();
    expect(pipe).toBeTruthy();
  });
});
